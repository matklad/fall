nodes {
  atom
  lparen rparen
  file
  list
}

tokenizer {
  whitespace r"\s+"
  lparen     r"\("
  rparen     r"\)"
  atom       r"\w+"
}

rule file { <rep sexp> }

rule sexp { atom | list }
rule list { lparen <rep sexp> rparen }
