nodes {
  atom
  lparen rparen
  file
  list
}

tokenizer {
  lparen     '('
  rparen     ')'
  whitespace r"\s+"
  atom       r"\w+"
}

rule file { <rep {sexp}> }

rule sexp { atom | list }
rule list { '(' <rep {sexp}> ')' }
