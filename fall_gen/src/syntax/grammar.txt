nodes {
  kw_nodes
  kw_tokenizer
  kw_rule

  eq pipe star
  lbrace rbrace
  langle rangle
  lparen rparen
  ident
  simple_string
  hash_string

  file
  string
  nodes_def
  tokenizer_def
  lex_rule
  syn_rule
  alt
  part
}

tokenizer {
    whitespace r"\s+"
    eq "="
    pipe r"\|"
    star r"\*"
    lbrace r"\{"
    rbrace r"\}"
    langle "<"
    rangle ">"
    lparen r"\("
    rparen r"\)"
    simple_string r#"r?"([^"\\]|\\.)*""#
    hash_string "r#+" "super::parse_raw_string"
    kw_nodes "nodes"
    kw_tokenizer "tokenizer"
    kw_rule "rule"
    ident r"\w+"
}
