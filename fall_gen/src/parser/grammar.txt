nodes = {
  nodes
  tokenizer_kw

  eq
  lbrace rbrace
  ident
  simple_string
  hash_string

  file
  string
  nodes_def
  tokenizer_def
  rule
}

tokenizer = {
    whitespace r"\s+"
    eq "="
    lbrace r"\{"
    rbrace r"\}"
    simple_string r#"r?"[^"]*""#
    hash_string "r#+\"" super::parse_raw_string
    nodes "nodes"
    tokenizer_kw "tokenizer"
    ident r"\w+"
}
