nodes = {
  kw_nodes
  kw_tokenizer

  eq
  lbrace rbrace
  ident
  simple_string
  hash_string

  file
  string
  nodes_def
  tokenizer_def
  rule
}

tokenizer = {
    whitespace r"\s+"
    eq "="
    lbrace r"\{"
    rbrace r"\}"
    simple_string r#"r?"([^"\\]|\\.)*""#
    hash_string "r#+" "super::parse_raw_string"
    kw_nodes "nodes"
    kw_tokenizer "tokenizer"
    ident r"\w+"
}
