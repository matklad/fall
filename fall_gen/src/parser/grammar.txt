nodes = {
  nodes
  tokenizer_kw

  eq
  lbrace rbrace
  ident
  string

  file
  nodes_def
  tokenizer_def
  rule
}

tokenizer = {
    whitespace r"\s+"
    eq "="
    lbrace r"\{"
    rbrace r"\}"
    string r#"r?"[^"]*""#
    nodes "nodes"
    tokenizer_kw "tokenizer"
    ident r"\w+"
}
