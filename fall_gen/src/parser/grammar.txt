nodes {
  kw_nodes
  kw_tokenizer
  kw_rule

  eq pipe start
  lbrace rbrace
  ident
  simple_string
  hash_string

  file
  string
  nodes_def
  tokenizer_def
  token_def
  rule_def
  alt
}

tokenizer {
    whitespace r"\s+"
    eq "="
    pipe r"\|"
    start r"\*"
    lbrace r"\{"
    rbrace r"\}"
    simple_string r#"r?"([^"\\]|\\.)*""#
    hash_string "r#+" "super::parse_raw_string"
    kw_nodes "nodes"
    kw_tokenizer "tokenizer"
    kw_rule "rule"
    ident r"\w+"
}
