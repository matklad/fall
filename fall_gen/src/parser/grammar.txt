nodes = {
  kw_nodes
  kw_tokenizer

  eq
  lbrace rbrace
  ident
  simple_string
  hash_string

  file
  string
  def_nodes
  def_tokenizer
  rule
}

tokenizer = {
    whitespace r"\s+"
    eq "="
    lbrace r"\{"
    rbrace r"\}"
    simple_string r#"r?"([^"\\]|\\.)*""#
    hash_string "r#+" "super::parse_raw_string"
    kw_nodes "nodes"
    kw_tokenizer "tokenizer"
    ident r"\w+"
}
